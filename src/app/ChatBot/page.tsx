import { ChatBot } from "./ChatBot";

export default function Page (){
    return (
        <main className="flex flex-col justify-center items-center w-full h-fit">
            <ChatBot/>
        </main>
    )
}